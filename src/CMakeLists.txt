# set(lib "src_lib")
# add_subdirectory(cam_util)

# add_library(${lib} INTERFACE)
# target_link_libraries(${lib} INTERFACE
#     cam_util_lib
# )

# target_include_directories(${lib} INTERFACE
#     ${opencv_include_dir}
# )